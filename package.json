{
  "name": "fernandofreitasmoreira.github.io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Site pessoal — vanilla static, Markdown + build script único.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "marked": "^15.0.0"
  }
}
